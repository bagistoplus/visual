<!DOCTYPE html>
<html lang="{{ app()->getLocale() }}">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ name }}</title>

    @stack('meta')

    @includeIf('partials.colors)
    @bagistoVite(['resources/assets/css/theme.css'])
    @stack('styles')
  </head>

  <body>
    <main>
      @visual_layout_content
    </main>

    @stack('scripts')
  </body>

</html>
